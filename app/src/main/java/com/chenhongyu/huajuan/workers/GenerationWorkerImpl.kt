package com.chenhongyu.huajuan.workers

/**
 * Placeholder file. WorkManager-based worker implementation was intentionally avoided in this environment
 * to keep static analysis simple. The real generation pipeline is exposed via GenerationManager.generateForId(context, id)
 * and Repository.enqueueAICreationGeneration currently enqueues a coroutine to call it.
 */
object GenerationWorkerImplPlaceholder {
    // Intentionally empty.
}
